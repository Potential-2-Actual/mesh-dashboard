# Mesh Dashboard Configuration

# Server
PORT=3100
ORIGIN=https://mesh.potential2actual.com

# Google OAuth
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Session
SESSION_SECRET=  # random 32+ bytes, used for HMAC cookie signing

# NATS
NATS_URL=wss://mesh.potential2actual.com/nats

# NATS Seeds (SEPARATE keys!)
# Browser NKey (dashboard-gp) — scoped to channel pub/sub + presence read
DASHBOARD_BROWSER_SEED=
# Server NKey (dashboard-server) — scoped to $JS.API.> for JetStream history
NATS_SERVER_SEED=

# Auth
ALLOWED_EMAILS=gp@potential2actual.com
